Ook de logging kunnen we aan of uit zetten met \textbf{on} of \textbf{off}
\begin{lstlisting}[language=bash]
sudo ufw logging on
\end{lstlisting}
Voor nu laten we de logging altijd aan. Want we willen kunnen zien wat er gebeurd.

