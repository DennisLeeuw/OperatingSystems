\begin{enumerate}
\item Wat is het verschil tussen een gebruiker en een groep in Linux?
\item Waarom gebruik je groepen bij het delen van bestanden?
\item Wat doet het commando:
\begin{lstlisting}[language=bash]
chmod
\end{lstlisting}
\item Wat doet het commando:
\begin{lstlisting}[language=bash]
chown
\end{lstlisting}
\item Wat doet het commando:
\begin{lstlisting}[language=bash]
chgrp
\end{lstlisting}
\item Wat betekent deze tekenreeks
\begin{lstlisting}[language=bash]
drwxr-s---
\end{lstlisting}
\item Wat is het doel van het setgid-bit op een gedeelde directory?
\item Waarom is het niet voldoende om alleen de gebruiker als eigenaar van een bestand in te stellen wanneer meerdere medewerkers met hetzelfde bestand moeten werken?
\item Wat gebeurt er als een gebruiker geen rechten heeft op een directory, maar wel rechten heeft op een bestand dat zich in die directory bevindt?
\item Waarom is het vanuit beveiliging belangrijk dat gebruikers alleen toegang krijgen tot de mappen die zij nodig hebben?
\end{enumerate}

