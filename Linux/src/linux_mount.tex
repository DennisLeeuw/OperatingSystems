Met het \texttt{mount}\index{mount}\index{commando!mount} commando kunnen we nieuwe verbindingen maken tussen een disk of partitie en het bestandssysteem. Er is een speciale directory waarop de root-gebruiker tijdelijk disks kan mounten, deze directory is \texttt{/mnt}\index{mnt}.

