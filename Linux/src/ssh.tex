SSH staat voor Secure Shell en is een methode om op een veilige manier in te loggen op systeem die werken met een command line interface. SSH gebruikt port 22 op de server voor de verbinding. SSL (PKI) wordt gebruikt voor de encryptie.

