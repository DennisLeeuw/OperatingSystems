Een mountpoint is een normale directory. Elke directory kan dus een mountpoint zijn. Het is handig om hiervoor een lege directory te maken. Als er al data in de directory staat en je mount op de directory een andere disk dan is de inhoud van de directory niet meer bereikbaar. Pas na het unmounten kan je dan weer bij je data.

